"""Synthetic experiment framework for Joint vs Two-layer routing comparison.

This package implements a clean, self-contained synthetic simulation that
tests all 5 selection mechanisms (cheapest / fastest / V2 / LP / hedge) in
both two-layer and joint architectures on controlled scenarios.

Main entry point: experiment.synthetic.runner.run_all
"""
